<UserControl x:Class="CafePos.Presentation.Views.OpenShiftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="White" Padding="40" CornerRadius="16" BorderBrush="#E0E0E0" BorderThickness="1">
            <StackPanel Width="320">
                <TextBlock Text="Open Shift" FontSize="28" FontWeight="Bold" Margin="0,0,0,12" />
                <TextBlock Text="Opening Float" />
                <TextBox Text="{Binding OpeningFloat, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Foreground="Crimson" Text="{Binding ErrorMessage}" Margin="0,6,0,0" />
                <Button Content="Open Shift" Command="{Binding OpenShiftCommand}" Margin="0,12,0,0" Height="56" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
