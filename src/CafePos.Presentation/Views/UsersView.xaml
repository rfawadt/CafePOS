<UserControl x:Class="CafePos.Presentation.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="40">
        <TextBlock Text="Users (MVP placeholder)" FontSize="26" FontWeight="Bold" />
        <Button Content="Back" Command="{Binding BackCommand}" Margin="0,60,0,0" Width="140" Height="50" />
    </Grid>
</UserControl>
